export default class Registered {
  constructor(id, courseId, studentId, grades, approved) {
    this.id = id;
    this.courseId = courseId;
    this.studentId = studentId;
    this.grades = grades;
    this.approved = approved;
  }
}
